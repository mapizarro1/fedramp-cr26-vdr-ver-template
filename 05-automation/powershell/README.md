# PowerShell automation

IRV mapper and access tests.

- Build-IrvMap.v3.ps1 - asset-exposure / internet-reachability mapper (CR26 T1)
- Test-IrvAccess.ps1 - reachability access checks
- Initialize-IrvScaffold.ps1 - run scaffold

Production evidence runs execute on the in-boundary automation host under managed identity. Workstation runs are validation only.

